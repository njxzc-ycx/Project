<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="45dp">

    <EditText
        android:id="@+id/editTextPwd"
        android:inputType="textPassword"
        android:layout_width="fill_parent"
        android:layout_height="45dp"
        />


    <ImageView
        android:id="@+id/img_delPwd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_clear_gray_24dp"
        android:layout_alignRight="@+id/editTextPwd"
        android:layout_alignTop="@+id/editTextPwd"
        android:layout_alignBottom="@+id/editTextPwd"
        android:visibility="gone"
        android:padding="5dp"
        />

    <ImageView
        android:id="@+id/img_eye"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/hidepwd"
        android:padding="5dp"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        android:visibility="gone"
        android:layout_marginEnd="23dp" />




</RelativeLayout>